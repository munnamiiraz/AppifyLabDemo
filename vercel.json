{
  "buildCommand": "cd backend && npm install && npm run vercel-build",
  "installCommand": "cd backend && npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ],
  "functions": {
    "api/index.ts": {
      "memory": 1024,
      "maxDuration": 10
    }
  }
}
