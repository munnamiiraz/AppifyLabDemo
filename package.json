{
  "name": "appifylab-demo-task",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "cd backend && npm install && npm run vercel-build",
    "build": "cd backend && npm install && npm run build"
  },
  "dependencies": {
    "@prisma/client": "^6.17.1",
    "express": "^4.18.2",
    "dotenv": "^16.3.1",
    "cors": "^2.8.5",
    "helmet": "^7.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.18",
    "@types/node": "^20.19.25",
    "typescript": "^5.2.2",
    "prisma": "^6.19.0"
  }
}
